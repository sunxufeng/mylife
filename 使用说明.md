# My Life · 使用说明

一个**完全离线**的桌面资料库：把经历、工作资料、阅读笔记和灵感收进同一个地方，
随时搜得到，还能一键导出成 Markdown 交给 AI 阅读。

- 技术：Electron + 原生 HTML/CSS/JavaScript（没有构建步骤）
- 数据：本机的一个目录（JSON + 附件文件），**不联网、不需要账号、不调用任何模型**
- 平台：macOS（Apple Silicon），已在 macOS 26.6.2 / arm64 实测
- 版本：1.1.0

---

## 一、打开应用

**方式 A：已安装的副本（推荐）**

应用装在 **`/Applications/My Life.app`**。在「启动台」「访达 → 应用程序」里双击，
或者按 `⌘空格` 搜「My Life」。

**方式 B：直接跑构建产物**

`dist/My Life-darwin-arm64/My Life.app`，双击同样能用。

> 因为是我们本机构建的（不是从网上下载的），文件上没有隔离属性，正常双击不会被 Gatekeeper 拦。
> 真要遇到「无法验证开发者」，右键点图标 → 打开 → 再点「打开」。

**方式 C：从源码运行**

```bash
cd "My Life"
npm install          # 首次
npm start
```

> 本机环境里预设了 `ELECTRON_RUN_AS_NODE=1`，`npm start` 已经写成
> `env -u ELECTRON_RUN_AS_NODE electron .` 把它剥掉；手动跑的话记得也剥一下，
> 否则 Electron 会退化成纯 Node 起不来。

**重新打包**

```bash
npm run icon    # 重新生成图标（可选）
npm run pack    # 产出 dist/My Life-darwin-arm64/My Life.app
```

---

## 二、界面

三栏布局，从左到右：

| 区域 | 内容 |
| --- | --- |
| 左栏 | 新建按钮、全部资料 / 回收站、分类、标签、资料库位置与「设置与配色」 |
| 中栏 | 搜索框、类型 / 标签 / 时间筛选、排序、多选与批量操作、资料列表 |
| 右栏 | 详情（标题、类型、日期、来源、分类、标签、链接、正文、附件），点「编辑」变成编辑表单 |

**两侧栏都能收起来**，给编辑区腾地方：

- 左栏顶部 `«` 收起分类栏 → 收成一条窄边，点边上的 `»` 展开
- 中栏右上角 `«` 收起列表栏，同理
- 快捷键 `⌘1` 收 / 放分类栏，`⌘2` 收 / 放列表栏
- 收起状态会记住，下次打开还是那样

---

## 三、配色主题

左下角「⚙ 设置与配色」，或按 `⌘,`。设置面板里有 5 套配色，点一下立刻生效：

**暖沙色系 · 雾蓝色系 · 陶土橙色系 · 鼠尾草绿系 · 深酒红色系**

主题只改配色（背景、文字、强调色），不影响任何数据和排版。选择会存进配置文件，下次打开还是这套。

设置面板里还有：备份与恢复（带备份历史列表）、资料库位置、显示选项、应用信息。

---

## 四、新建与编辑

点左上角「＋ 新建资料」，先选类型：

- **文字**：随手记的经历、想法、片段（默认用 Markdown 编辑器）
- **链接**：存网页地址，再写下自己的摘要和看法
- **图片**：选一张或多张图，可附说明
- **文件**：PDF、Word 等任意文件

**图片和文件会被复制一份进资料库**，你的原始文件原地不动、删掉也不影响。

### Markdown 编辑器

编辑区右上角有「纯文本 / Markdown」切换。切到 Markdown 后：

- 上面一排工具栏：`H1 H2 H3`／`B I S 代码`／`列表 编号 待办 引用`／`链接 表格 代码块 分割线`
- 右边是**实时预览**，边写边看效果，可以点「隐藏预览」关掉
- 工具栏最右的「专注」：把左右两栏一起收起，只剩编辑区（临时状态，退出即恢复）
- 正文里 `⌘B` 加粗、`⌘I` 斜体、`Tab` 缩进

支持的语法：标题、**粗体**、*斜体*、~~删除线~~、==高亮==、`` `行内代码` ``、代码块、
引用、有序 / 无序 / 任务列表（含复选框）、表格、分割线、链接、图片。
Markdown 原文按原样存在 `archive.json` 里，导出时也是原文——不会被转成 HTML 存起来。

纯文本模式照样能用（正文框也加高到 420px），两种模式互相切换不会丢内容。

### 其它编辑操作

- 字段：标题、内容、日期、来源、分类、标签（可自由增删）
- 附件三种加法：拖进虚线框、「选图片 / 选文件」按钮、正文里直接 `⌘V` 粘贴截图
- 改完 `⌘S` 保存，`Esc` 取消（取消时本次新加的附件副本会被清掉，不留垃圾）

---

## 五、查找与使用

**搜索** — 搜索框同时匹配标题、正文、来源、链接、分类、标签和附件名，命中的词在列表里高亮。
Markdown 资料按渲染后的纯文本参与搜索和摘要显示，`#` `**` 这类标记不会干扰结果。

**筛选与排序** — 下面四个下拉分别按类型、标签、时间范围筛选和排序；「清除筛选」一键复位。

**回收站** — 删除的资料先进回收站，可以还原，也可以彻底删除或「清空回收站」。彻底删除会连附件副本一起清掉。

**导出 Markdown** — 列表里勾选若干条（或全选），点「导出 Markdown」：可合并成一个文件或每条一个文件，
可选是否把附件一起复制过去，导出前能「预览内容」。导出的 Markdown 保留标题、类型、日期、来源、
分类、标签、链接、正文和附件清单。单条资料也能直接「复制 Markdown」。

**备份与恢复** — 设置面板里：

- **立即备份**：打包成 zip 直接存进资料库的 `backups/` 目录，下面会列出历史备份
- **备份到其他位置**…：存到你想放的地方（比如移动硬盘）
- **从备份恢复**…：先展示备份里有多少条资料、多少文件，确认后整体替换当前资料库
- 备份历史里每一条都能「用这个恢复」

> 恢复前，当前内容会自动留存到 `backups/恢复前_时间戳/` 作为保险副本，误操作也能找回。

**更换资料库位置** — 设置里「更换」。注意：换目录后应用会去新目录读数据，
**原目录里的资料不会自动搬过去**，需要你自己把 `archive.json` 和 `attachments/` 拷到新目录。

---

## 六、数据放在哪

默认资料库：**`~/Documents/My Life 资料库/`**（首次启动自动创建）

```
My Life 资料库/
├── archive.json          全部文字资料（标题、正文、格式、来源、标签、附件索引…）
├── archive.json.bak      上一次写入前的自动备份
├── attachments/<资料id>/ 附件的副本，按资料分组
└── backups/              备份包与恢复保险副本
```

配置与偏好（资料库路径、主题、各栏收起状态）在 `~/Library/Application Support/My Life/config.json`。

**数据独立于安装位置**：删除或重装 `My Life.app`，甚至把 app 拷到别的电脑，资料都还在。
写入采用「先写临时文件再改名」的原子方式，并保留 `.bak`；万一 `archive.json` 损坏，
下次启动会自动从 `.bak` 恢复。如果所在磁盘不允许改名（某些同步网盘 / 安全软件会拦），
会自动退化成直接覆盖写，不会丢这次修改。

### 从旧版「人生档案馆」迁移

1.1.0 之前这个应用叫「人生档案馆」，数据目录是 `~/Documents/人生档案馆/`。
**第一次启动 1.1.0 时，如果新目录还不存在、旧目录里有数据，应用会把旧目录整个改名搬过来**
（目录改名是原子操作，数据不会丢），并提示一句「资料库已随改名迁到 My Life 资料库」。
同时，那条旧版的使用说明也会被替换成当前版本的文案（只在你没改过它标题的情况下）。
如果你之前手动改过资料库位置，那就保持原样不动。

---

## 七、快捷键

| 快捷键 | 作用 |
| --- | --- |
| `⌘N` | 新建资料 |
| `⌘F` | 聚焦搜索框 |
| `⌘S` | 保存当前编辑 |
| `⌘,` | 打开设置 |
| `⌘1` / `⌘2` | 收起 / 展开分类栏 / 列表栏 |
| `⌘B` | 立即备份到资料库 |
| `⌘⇧O` | 打开资料库目录 |
| `⌘B` / `⌘I`（编辑器内） | 加粗 / 斜体 |
| `Esc` | 取消编辑 / 关闭弹层 |

---

## 八、明确不做的部分

按约定，以下内容**不在**本应用范围内：账号系统、云同步、模型接口、自动抓取网页、
解析附件内容（只存文件，不读里面的文字）。

---

## 九、源码结构

```
My Life/
├── package.json
├── src/
│   ├── main/
│   │   ├── main.js        主进程：窗口、菜单、archive:// 协议、全部 IPC
│   │   ├── store.js       数据层：archive.json 原子读写、附件复制、回收站、分类、旧版说明升级
│   │   ├── library.js     备份 / 恢复 / Markdown 导出 / 目录遍历 / 旧目录迁移
│   │   └── devtools.js    开发期自测钩子（不设环境变量就不加载）
│   ├── preload/preload.js 受控地暴露一组 IPC 能力（contextIsolation 打开）
│   └── renderer/
│       ├── index.html     三栏骨架与可收缩结构
│       ├── styles.css     视觉：暖白纸 / 墨色字 / 主题变量
│       ├── themes.js      5 套配色主题定义
│       ├── markdown.js    轻量 Markdown 渲染器（先转义再套规则，无依赖）
│       └── app.js         界面逻辑
├── build/                 图标源文件与生成脚本
└── scripts/
    ├── smoke-test.js      数据层自测（68 项断言）
    ├── ui-check.js        界面端到端自测
    └── ui-verify.js       打包产物启动 / 持久化验证
```

自测怎么跑：

```bash
node scripts/smoke-test.js

LA_LIBRARY_PATH=/tmp/la-test LA_SHOT_DIR=/tmp/la-shots LA_TEST_SCRIPT=scripts/ui-check.js \
  env -u ELECTRON_RUN_AS_NODE ./node_modules/.bin/electron . --no-sandbox --disable-gpu
```

（`LA_LIBRARY_PATH` 把资料库指到临时目录，自测就不会写进真实的「文稿/My Life 资料库」。）

---

## 十、实测结果与未验证事项

### 已实测通过

**数据层自测（68 项断言全绿）**：资料库自动创建、四种类型新建、附件复制入库且原始文件不动、
同名附件自动加序号、剪贴板二进制转附件、编辑与标签去重、统计口径、回收站移入/还原/彻底删除
（连带清附件）、模拟重启后仍能读到内容、Markdown 导出（含内容与来源、附件副本随导出落地）、
备份 zip、破坏后恢复且内容与备份完全一致、恢复前自动留存保险副本、分类改名/删除、
路径越界拒绝、`archive.json` 损坏时自动从 `.bak` 恢复、旧版说明随改名升级（且不误伤自己改过的条目）、
`rename` 被拒时退化成直接覆盖写而不丢数据。

**界面端到端自测（驱动真实窗口，全部通过）**

- 真实 IPC 新建 5 条资料（文字 / 链接 / 图片 / 文件），中文文件名附件正常
- 点击卡片进详情、编辑并保存、标题与标签落库
- 编辑态 `⌘V` 粘贴图片 → 成为附件；拖拽落区高亮与释放；真实磁盘文件拖入后被复制成附件
- 取消编辑后本次新增的附件副本被清掉，改动没有落进资料
- 搜索匹配标题与正文、关键词高亮；类型筛选、标签筛选、日期排序、勾选与批量操作栏
- 导出预览的 Markdown 文本（标题 / 类型 / 日期 / 来源 / 分类 / 标签 / 正文齐全）
- 回收站移入与还原、新建分类、复制 Markdown 到剪贴板
- **配色主题**：设置里 5 套主题可切换、CSS 变量立即变化、选中态正确、偏好写入 config.json
- **一键备份**：备份 zip 落到 `backups/` 且命名带 “My Life 备份_”，设置里的备份列表正确列出
- **两栏收缩**：分类栏 238→38→238px、列表栏 344→38→344px，收起后出现展开按钮，偏好正确复位
- **Markdown 编辑器**：默认进入 Markdown 模式、编辑区占满右栏（829/838px）、输入框高 460px、
  实时预览渲染出 H1/STRONG/EM/CODE/LI/BLOCKQUOTE、工具栏的表格 / H2 / 加粗都生效并反映到预览、
  预览可隐藏 / 显示、纯文本与 Markdown 互切不丢内容、`format` 随资料存下、
  详情页按 Markdown 渲染（标题 + 表格 + 任务列表复选框）
- **专注编辑**：两栏一起收起，右栏从 838px 扩到 1344px，且不改动偏好
- 备份（518 KB / 4 个文件）→ 改乱现状 → 恢复，内容与备份逐条一致，保险副本存在
- 越界附件路径（`../../etc/passwd`）被服务端拒绝
- 全程渲染进程零报错

**打包产物**：`dist/My Life-darwin-arm64/My Life.app`（bundle id `com.arete.mylife`，版本 1.1.0），
连续启动两次，第二次读到第一次写入的数据。

**旧目录迁移**：实测在 `~/Documents/` 里把 `人生档案馆/` 迁移到 `My Life 资料库/`，
资料条数与内容完整保留；旧版使用说明条目被自动更新成新版文案。

### 未验证事项（如实说明）

1. **原生系统对话框的最后一步没有自动点掉**：「打开文件」「保存备份到其他位置」「选择导出目录」。
   对话框本身弹出正常，但需要人手点确定；对话框之后的写盘逻辑（备份、导出、另存附件）
   已通过直接传路径 + 数据层自测覆盖。
2. **真实鼠标拖拽的文件路径提取**：合成事件里的 File 没有磁盘路径，`webUtils.getPathForFile`
   只对操作系统真实拖拽产生的 File 生效。已验证落区高亮、事件接线，以及拿到路径之后的复制逻辑
   （同一个处理函数直接喂真实路径），但「从访达拖进来」这一步需要你亲手试一次。
3. **窗口可见性不由我确认**：自动化环境里只能确认主进程与渲染进程都起来了、界面渲染无报错，
   画面是否真的显示在你屏幕上需要你亲眼看一下。
4. 「更换资料库位置」依赖原生目录选择器，未走完全流程。
5. 只在 Apple Silicon + macOS 26.6.2 上验证，没在 Intel Mac / Windows / Linux 上跑过。
6. 打包产物未做代码签名与公证，首次打开可能需要右键 → 打开。
